---
import Footer from '../components/Footer.astro';
import { main } from './PageLayout.css';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<Fragment>
	<header></header>
	<main class:list={[main, className]}>
		<slot />
	</main>
	<Footer />
</Fragment>
