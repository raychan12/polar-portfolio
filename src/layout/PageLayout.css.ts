import { style } from '@vanilla-extract/css';

export const main = style({
	flexGrow: 1,
	width: '100%',
	maxWidth: '1200px',
	padding: '120px',
	marginInline: 'auto',
});

export const smallTopPadding = style({
	paddingTop: '80px',
});
